clear
clc
i= input('Intriduce un numero: ')
    if mod (i,2)==0
        
        disp(['EL numero par elevado es: ', num2str(a*a)]);
    else 
        total:a*a*a;
        disp(['EL numero impar elevado es: ', num2str(a*a*a)]);
    end